## jit agent run

Run the agent in the foreground (normally started by launchd, not by hand)

```
jit agent run [flags]
```

### Options

```
      --ttl duration   how long an unlocked session stays cached before auto-locking (default 15m0s)
```

### SEE ALSO

* [jit agent](jit_agent.md)	 - Run a background helper so you only unlock once, not once per command

