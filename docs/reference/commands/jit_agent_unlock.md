## jit agent unlock

Unlock the running agent's session now (prompts Touch ID if needed)

### Synopsis

Pre-warms the shared session so a run of jit run/vault get/export right after doesn't prompt.

```
jit agent unlock
```

### SEE ALSO

* [jit agent](jit_agent.md)	 - Run a background helper so you only unlock once, not once per command

