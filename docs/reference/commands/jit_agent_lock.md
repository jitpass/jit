## jit agent lock

Lock the running agent's session immediately, without waiting for the TTL

```
jit agent lock
```

### SEE ALSO

* [jit agent](jit_agent.md)	 - Run a background helper so you only unlock once, not once per command

