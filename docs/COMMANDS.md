# Moved

The command reference now lives at
**[docs/reference/commands/](./reference/commands/jit.md)**, one page per
command, generated from the CLI itself so it can never drift from
`jit <command> --help`.

This stub will be removed in a future release.
